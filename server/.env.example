# MongoDB connection string (Atlas, self-hosted, etc.)
MONGODB_URI=mongodb+srv://<db_username>:HRenCWOXOPCd4YLc@cluster0.i3kpu9b.mongodb.net/

# Server
PORT=5000
CLIENT_URL=http://localhost:5173

# Auth
JWT_SECRET=2xB1LDjks9V8tXsuk7S4MS9TA2iOE3HQvf5El281VFW
JWT_EXPIRES_IN=7d

# Seeded admin login (used only by `npm run seed`)
ADMIN_USERNAME=loscale admin
ADMIN_PASSWORD=Pardon@2026
ADMIN_EMAIL=grow@loscaledigital.com

# ---- Email (optional, disabled until you fill this in) ----
# Leave EMAIL_ENABLED=false to skip sending mail; submissions are still saved to the database.
# When you're ready, set EMAIL_ENABLED=true and fill in SMTP creds (Gmail, Zoho, Resend SMTP, etc.)
EMAIL_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_NOTIFICATION_EMAIL=grow@loscaledigital.com
